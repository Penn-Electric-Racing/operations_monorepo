name: Poll to Notion

on:
  schedule:
    - cron: "*/15 * * * *"
    - cron: "40 4 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: notion-poll
  cancel-in-progress: false

jobs:
  poll:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Sync recently updated issues and PRs
        working-directory: notion-github-sync
        env:
          SINCE_MINUTES: ${{ github.event.schedule == '40 4 * * *' && '0' || '1440' }}
          BACKFILL_STATE: ${{ github.event.schedule == '40 4 * * *' && 'open' || 'all' }}
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GH_READ_TOKEN }}
        run: node scripts/backfill.mjs
